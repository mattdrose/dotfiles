# Shopify `dev` Tool
if [ -f /opt/dev/dev.sh ]
then 
	source /opt/dev/dev.sh
fi

# Spin
alias s='spin'

alias su='spin up'
alias suw='spin up web'

alias ss='spin shell'

alias sc='spin code'

alias so='spin open'
